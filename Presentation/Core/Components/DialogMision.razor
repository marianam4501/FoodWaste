<MudDialog>
    <DialogContent>
        Nuestro principal objetivo es que de forma solidaria y comprometida podamos proporcionar alimentos y apoyar a las poblaciones vulnerables que por alguna razón no puedan 
        acceder a diferentes tipos de alimentos. De esta forma se apoyan a las comunidades de Costa Rica mientras se combate el foodwaste.
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Primary" OnClick="Submit">Volver</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    void Submit() => MudDialog.Close(DialogResult.Ok(true));
    void Cancel() => MudDialog.Cancel();
}