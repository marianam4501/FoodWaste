@using Presentation.Orders.Components;

@code {
	[Fact]
	public void ComponentDisplaysNameCorrectly()
	{
		var context = new TestContext();
		var cut = context.Render(@<BeneficiaryInfoCard BeneficiaryName="David"></BeneficiaryInfoCard>);
		cut.Markup.Should().Contain("David");
	}
}
