<!-----------------------------------------------------------------------------
    This document is for the US:
    - ALV-DO-4.1 Create a donation campaign
    And was done by a joint effort of members of ALV.
-------------------------------------------------------------------------------
    Displays product's information as read-only.
------------------------------------------------------------------------------>

@* Project includes *@
@using Application.Products
@using Domain.Campaigns.Entities
@using Presentation.Core.Components
@using Presentation.Products.Components
@using Presentation.Products.Models

@* System includes *@
@using System.Linq;

@* Injecting project services *@
@inject IDialogService DialogService

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h5" Align="Align.Center">
            Datos del Producto
        </MudText>
    </TitleContent>
    <DialogContent >
        <MudContainer Style=" max-height: 500px; overflow-y: scroll">
            <center>     
                <!-- Product Name -->        
                <MudItem md="30" lg="12">
                    <MudTextField @bind-Value="product!.Name" Label="Nombre"
                        Style=@($"--mud-palette-primary:{Colors.Blue.Accent3}")
                        Immediate="true" 
                        Variant="Variant.Outlined" Margin="Margin.Normal"
                        ReadOnly="true">            
                    </MudTextField>
                </MudItem>

                <MudGrid Spacing="3" Justify="Justify.Center">
                    <!-- Food Type --> 
                    <MudItem md="20" lg="6">
                        <MudTextField @bind-Value="product!.FoodType"
                        Style=@($"--mud-palette-primary:{Colors.Blue.Accent3}")
                        Dense="true" Label="Tipo de alimento" ReadOnly="true"
                        Variant="Variant.Outlined" Margin="Margin.Normal">
                        </MudTextField>
                    </MudItem>
                    <!-- Product Type --> 
                    <MudItem md="20" lg="6">
                        <MudTextField @bind-Value="product!.ProductType"
                        Style=@($"--mud-palette-primary:{Colors.Blue.Accent3}")
                        Label="Tipo de producto" Variant="Variant.Outlined"
                        ReadOnly="true" Margin="Margin.Normal" Dense="true">
                        </MudTextField>
                    </MudItem>
                </MudGrid>

                <MudGrid Spacing="3" Justify="Justify.Center">
                    <!-- Product Goal --> 
                    <MudItem md="4" lg="4">
                        <MudTextField @bind-Value="product!.Goal"
                            Style=@($"--mud-palette-primary:{Colors.Blue.Accent3}")
                            Label="Meta de recolección" Immediate="true" ReadOnly="true"
                            Variant="Variant.Outlined" Margin="Margin.Normal">
                        </MudTextField>
                    </MudItem>
                    <!-- Product Quantity --> 
                    <MudItem md="4" lg="4">
                        <MudTextField @bind-Value="product!.Quantity"
                            Style=@($"--mud-palette-primary:{Colors.Blue.Accent3}")
                            Label="Donación inicial" Immediate="true" ReadOnly="true"
                            Variant="Variant.Outlined" Margin="Margin.Normal">
                        </MudTextField>
                    </MudItem>
                    <!-- Product Weight --> 
                    <MudItem md="4" lg="4">
                        <MudTextField @bind-Value="product!.Weight" 
                            Style=@($"--mud-palette-primary:{Colors.Blue.Accent3}")
                            Label="Peso deseado (g)" Immediate="true" ReadOnly="true"
                            Variant="Variant.Outlined" Margin="Margin.Normal">
                        </MudTextField>
                    </MudItem>
                </MudGrid>
            </center>
            <br>
        </MudContainer>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel" Variant="Variant.Filled" 
            Color="Color.Success"> Ok </MudButton>
    </DialogActions>
</MudDialog>

@code {
    // Parameters
    // private bool _isLoading = true;
    [Parameter] public CampaignProduct? product { get; set; }
    [CascadingParameter] MudDialogInstance? MudDialog { get; set; }

    void Cancel() => MudDialog!.Cancel();
}