<MudDialog>
    <DialogContent>
        Como iniciativa moderna e inclusiva deseamos formar una comunidad en la que todos tengan acceso a suficientes alimentos para mantenerse saludables, activos y que así 
        puedan llevar a cabo sus actividades diarias de la mejor manera.
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Primary" OnClick="Submit">Volver</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    void Submit() => MudDialog.Close(DialogResult.Ok(true));
    void Cancel() => MudDialog.Cancel();
}
